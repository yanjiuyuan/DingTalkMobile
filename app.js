import lib from "/lib.js";
App({
    onLaunch(options) {
        return;
    },
    onShow() {},
    onHide() {},
    userInfo: null,
    globalData: {
        hasLogin: false,
        appId: 189694580,
        table: {},
        valid: false //表示全局table变量是否可用
    }
});
